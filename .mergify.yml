queue_rules:
  - name: default
    merge_method: squash
    commit_message_template: |
      {{ title }} (#{{ number }})

      {{ body | get_section("## Summary", "") }}

pull_request_rules:
  - name: auto-queue PRs targeting main
    conditions:
      - base = main
    actions:
      queue:
        name: default
