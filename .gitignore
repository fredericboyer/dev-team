node_modules/
dist/
.DS_Store
*.log
coverage/
.env
.env.*

# Runtime state files (created by task loop, not committed)
.dev-team/task.json
.dev-team/.memory-reviewed
.dev-team/agent-status/

# Claude Code runtime artifacts
.claude/worktrees/

# Project-specific hooks are tracked (not ignored)
# .claude/hooks/ is NOT ignored — these are project customizations

# npm
npm-debug.log*
.npm

# OS
Thumbs.db
